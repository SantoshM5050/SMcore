{
  "name": "smcore",
  "version": "1.0.0",
  "private": true,
  "description": "SMCore — Premium Discord Moderation & Community Management Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:bot": "npm run dev --workspace=apps/bot",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev": "npm run dev:bot",
    "build:packages": "npm run build --workspace=packages/shared && npm run build --workspace=packages/database",
    "build:bot": "npm run build --workspace=apps/bot",
    "build:web": "npm run build --workspace=apps/web",
    "build": "npm run build:packages && npm run build:bot && npm run build:web",
    "lint": "npm run lint --workspace=packages/shared && npm run lint --workspace=packages/database && npm run lint --workspace=apps/bot && npm run lint --workspace=apps/web",
    "test": "npm run test --workspace=packages/shared && npm run test --workspace=packages/database && npm run test --workspace=apps/bot",
    "db:generate": "npm run generate --workspace=packages/database",
    "db:push": "npm run db:push --workspace=packages/database",
    "db:migrate": "npm run db:migrate --workspace=packages/database",
    "db:studio": "npm run db:studio --workspace=packages/database"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
